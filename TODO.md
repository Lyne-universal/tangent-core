# TODO - Tangent repo fixes

- [ ] Fix redaction bug in `tangent-core/tangent_core.py` (duplicate key + HOME duplication).

- [ ] Remove unused `import os` from `tangent-core/tangent_core.py`.
- [ ] Expand root `README.md` with Features/Installation/Usage/CLI/GUI/Example output/Project structure.
- [ ] Expand `tangent-core/README.md` similarly.
- [ ] Ensure `tangent-core/LICENSE` exists (add MIT if missing).
- [ ] Ensure `tangent-core/requirements.txt` exists (include `customtkinter`).
- [ ] Ensure `tangent-core/.gitignore` includes typical Python patterns.
- [ ] Add at least one unit test under `tangent-core/tests/` verifying redaction output.
- [ ] Run tests + quick manual scan command to verify outputs.

